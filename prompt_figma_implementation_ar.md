# برومبت تنفيذ تصميم مطابق لملف Figma

انسخ النص التالي كما هو إلى أداة البرمجة أو وكيل التصميم، وأرفق معه ملف الـFigma بصيغة PDF وملف المشروع المضغوط.

---

أنت مهندس Frontend ومصمم UI/UX محترف. لديك ملف مشروع React مرفق، بالإضافة إلى ملف PDF صادر من Figma يمثل التصميم المرجعي المطلوب. مهمتك هي **إعادة تنفيذ واجهة المشروع بالكامل لتطابق ملف Figma بصريًا واحترافيًا**، وليس إجراء تعديلات سطحية على الألوان أو إضافة بعض المسافات.

اعتبر ملف Figma هو المرجع الأساسي والملزم في كل ما يتعلق بالهوية البصرية، تخطيط الصفحات، أبعاد العناصر، التباعد، المحاذاة، الحالات، التسلسل البصري، نمط الأزرار، الحقول، البطاقات، الشريط الجانبي، الترويسة، الأيقونات، ألوان الحالات، وأحجام الخطوط. لا تعتمد على التصميم الحالي إذا كان مختلفًا عن Figma، ولا تفترض أن المظهر الحالي صحيح.

## المطلوب أولًا: تحليل قبل التعديل

افحص جميع ملفات المشروع والمكوّنات ومسارات التنقل قبل كتابة الكود. افحص كل صفحات ملف Figma بالترتيب، وسجّل الفروقات بين التصميم الحالي والتصميم المرجعي. حدّد لكل صفحة الهيكل العام، عرض الشريط الجانبي، عرض منطقة المحتوى، أحجام العناوين، نمط النصوص الثانوية، نظام التباعد، نصف قطر الحواف، سماكة الحدود، الظلال، ألوان الحالات، وحالات hover وfocus وdisabled وempty وerror وloading.

لا تبدأ بالتعديل قبل فهم العلاقة بين الشاشات التالية: شاشة Evidence Status، شاشة Add Evidence، شاشة Evidence Detail، شاشة Reviewer Dashboard، وشاشة Error State. يجب أن تبدو هذه الصفحات وكأنها جزء من **منتج واحد صُمّم بنظام Design System موحّد**.

## الاتجاه البصري الإلزامي

طبّق أسلوب Figma كما هو: واجهة داخلية احترافية، نظيفة، هادئة، ومبنية على مساحة بيضاء واسعة مع شريط جانبي داكن عند الحاجة. استخدم تباينًا واضحًا لكن غير قاسٍ، وحدودًا رفيعة، وظلالًا خفيفة جدًا، وحواف مستديرة متناسقة. تجنّب البطاقات الثقيلة، الألوان العشوائية، التدرجات غير الموجودة في Figma، النصوص كبيرة الحجم بلا داعٍ، الفراغات غير المتوازنة، والعناصر المضغوطة.

استخدم خطًا حديثًا وواضحًا قريبًا من الخط الظاهر في Figma، مثل Inter أو الخط المحدد في المرجع. يجب توحيد وزن الخطوط بين 400 و500 و600 و700، مع استخدام العناوين والنصوص الثانوية بطريقة هرمية واضحة. لا تستخدم أحجامًا عشوائية لكل عنصر.

أنشئ نظام Tokens مركزيًا للألوان والمسافات ونصف قطر الحواف والظلال وأحجام النصوص. لا تكرر قيم الألوان عشوائيًا داخل المكوّنات. استخدم أسماء واضحة مثل `--color-primary` و`--color-surface` و`--color-border` و`--color-muted` و`--color-success` و`--color-warning` و`--color-danger`.

## تنفيذ الصفحات

في شاشة **Evidence Status**، نفّذ الترويسة والعنوان والوصف وزر Add Evidence كما تظهر في Figma. أضف شريط الفلاتر أو التبويبات بتصميم مطابق، مع عدادات الحالات ومحاذاة دقيقة. اجعل كل عنصر دليل يظهر في بطاقة أو صف واضح يحتوي على المهارة، نوع الدليل، الوصف، الرابط، تاريخ الإرسال، حالة المراجعة، وملاحظة المراجع عند وجودها. يجب أن تكون الحالة مرئية باستخدام Badge دلالي واضح، مع ألوان متناسقة لـApproved وPending وRejected وRevision. أضف البحث أو التحكم الموجود في المرجع فقط، ولا تضف عناصر غير موجودة بلا سبب تصميمي.

في شاشة **Add Evidence**، طبّق تخطيط Figma بالحرف: الشريط الجانبي، معلومات المهارة، مستوى المهارة، نسبة الثقة، تأثير الموافقة، عناصر التنقل أو التقدم، ثم منطقة المحتوى البيضاء. اجعل تبديل Reference URL وFile Upload واضحًا، واجعل خيارات GitHub Repository وLive Deployment وDesign File وDocument تظهر كمكوّنات اختيار متناسقة. طبّق حالات hover وactive وfocus وerror وdrag-and-drop. يجب أن تكون الحقول والأزرار بنفس ارتفاعات وحواف وألوان المرجع. حافظ على التحقق الحالي من الرابط والملف والوصف، لكن حسّن عرض رسائل الخطأ بصريًا لتطابق Figma.

في شاشة **Evidence Detail**، اجعل الصفحة مقسّمة كما في المرجع إلى معلومات الدليل وحالة المراجعة. رتّب العنوان، نوع الدليل، الرابط، الوصف، سجل التدقيق، بطاقة الحالة، ملاحظة المراجع، وتأثير الثقة بطريقة واضحة ومتوازنة. استخدم لوحات حالة مختلفة لـApproved وPending وRejected وRevision، مع ألوان دلالية هادئة ومطابقة لنظام الألوان.

في شاشة **Reviewer Dashboard**، طبّق الشريط الجانبي أو قائمة Review Queue بنفس أسلوب Figma، مع إبراز العنصر المحدد دون ألوان ثقيلة. نظّم منطقة تفاصيل الدليل ومنطقة قرار المراجع على شبكة واضحة. اجعل خيارات Approve وRequest Revision وReject مكوّنات اختيار متناسقة، وأظهر النص التوضيحي لكل خيار. حافظ على شرط كتابة الملاحظات عند طلب التعديل أو الرفض، وعلى زر Submit Review Decision وسلوك Skip Review Later.

في شاشة **Error State**، استخدم نفس الترويسة والمسافات واللغة البصرية، مع رسالة خطأ واضحة، زر إعادة المحاولة، ورابط أو إجراء التواصل مع الدعم. لا تجعل شاشة الخطأ مختلفة بصريًا عن بقية المنتج.

## الحفاظ على الوظائف

لا تحذف أي وظيفة موجودة. حافظ على التنقل بين المسارات، إضافة الدليل، إعادة الإرسال، فتح التفاصيل، الفلاتر، البحث، رفع الملفات، التحقق من المدخلات، قرارات المراجع، تحديث الحالة، وتحديث نسبة الثقة. أصلح أي أخطاء تشغيلية موجودة، مثل مراجع React أو hooks غير المستوردة، أخطاء الروابط، أو الحالات التي تسبب crash عند وجود بيانات ناقصة.

اجعل العناصر التفاعلية قابلة للاستخدام بلوحة المفاتيح، وأضف focus ring ظاهرًا، وlabels صحيحة للحقول، وARIA مناسبًا للتبويبات والأزرار والمكوّنات التي تتصرف كعناصر تفاعلية. لا تستخدم `div` تفاعليًا إذا كان من الممكن استخدام `button` أو `a` بشكل صحيح.

## الجودة والتجاوب

يجب أن تكون النتيجة متجاوبة بالكامل على الشاشات الكبيرة والمتوسطة والهاتف. في الهاتف، حوّل الشريط الجانبي إلى قسم علوي أو تنقل قابل للطي بطريقة منطقية، واجعل الجداول أو البطاقات قابلة للقراءة دون تمرير أفقي غير ضروري. لا تسمح بتكسر النصوص أو خروج الأزرار من الشاشة.

استخدم مكوّنات قابلة لإعادة الاستخدام بدل تكرار الكود. أنشئ مكوّنات مشتركة على الأقل للحالات، الأزرار، الحقول، الأيقونات، البطاقات، الشريط الجانبي، والترويسة إذا كان ذلك مناسبًا. يجب أن تكون أسماء المكوّنات واضحة، وأن يبقى الكود منظمًا وسهل الصيانة.

أضف انتقالات خفيفة وسريعة للحالات التفاعلية، بمدة لا تتجاوز 200–300ms، دون مبالغة في الحركة. احترم `prefers-reduced-motion`.

## التحقق النهائي الإلزامي

بعد التنفيذ، شغّل build المشروع وتأكد من عدم وجود أخطاء. اختبر كل مسار وكل زر وكل نموذج إدخال. راجع التصميم على سطح المكتب والهاتف. قارن النتيجة بصريًا مع ملف Figma، وليس فقط من خلال نجاح البناء.

أصلح أي فرق واضح في المحاذاة أو الأحجام أو الألوان أو التباعد أو ترتيب المكوّنات قبل اعتبار المهمة منتهية. لا تسلّم نسخة تجريبية أو واجهة عامة تشبه التصميم بشكل تقريبي. المطلوب نتيجة احترافية، متماسكة، وقريبة جدًا من ملف Figma في جميع الشاشات.

في النهاية، قدّم ملخصًا قصيرًا يذكر الصفحات التي تم تعديلها، نظام التصميم الذي تم توحيده، الوظائف التي تم الحفاظ عليها، وأي فروقات لا يمكن تنفيذها بسبب عدم وضوحها في ملف Figma.

**القيود المهمة:** لا تغيّر منطق المنتج دون سبب، لا تحذف بيانات الاختبار، لا تستبدل التصميم بواجهة عامة جاهزة، لا تستخدم ألوانًا أو مكوّنات لا يدعمها المرجع، ولا تعتبر نجاح build دليلًا كافيًا على اكتمال المهمة. معيار النجاح هو التطابق البصري والوظيفي مع Figma.

---

## معلومات المشروع

الملفات المرفقة هي المصدر الوحيد للكود والمرجع البصري. ابدأ بفحصها مباشرة، ثم نفّذ التعديلات داخل المشروع نفسه، ولا تنشئ مشروعًا منفصلًا أو mockup غير قابل للتشغيل.
